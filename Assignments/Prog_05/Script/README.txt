Usage:	./script.sh DATA_FOLDER_PATH OUTPUT_FILENAME
Notes: It will not create a directory if the OUTPUT_FILENAME is a nonexistant path.
	   It is assumed script.sh is used in the same directory as prog05 as it does not have a path to it.